program zmienne;
#include( "stdlib.hhf" );
    static
        a: int16;
        b: int16 := 10;
        wynik: int16;
    begin zmienne;
        stdin.get(a);
        mov(a, ax);
        mov(a, b);
        stdin.get(b);
        mov(b, ax);
        add(a, ax);
        mov(ax, wynik);
        // mov(a, ah);
        // mov(b, al);
        // add(al, ah);
        // mov(ah, wynik);
        stdout.put(wynik);
end zmienne;
